# Config reference

Placeholder — populated at M2. Will document the full schema of
`~/.config/nakli/crate-agent.toml` (Linux/macOS) /
`%APPDATA%\nakli\crate-agent.toml` (Windows): `[agent]`, `[identity]`,
`[crate]` sections, including encrypted pairing-token storage and
the v1.3 multi-folder `[[crate]]` migration path.
