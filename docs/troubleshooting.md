# Troubleshooting

Placeholder — populated at M7 alongside `status` and `doctor`. Will cover the top failure modes: token expiry, capability revocation, transport unreachable, filesystem permission denied, conflict folder noise, and per-platform service-installation gotchas.
