// SPDX-License-Identifier: AGPL-3.0-or-later
// Package config loads ~/.config/nakli/crate-agent.toml (or platform
// equivalent — see paths.go). Lands at M2 alongside `crate-agent pair`.
package config
